[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every unknown route serves index.html so the client router
# can handle it (including /auth/callback).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
